<UserControl x:Class="Clickers.Views.SoldierView.SoldierProducerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Clickers.Views.SoldierView"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>
        <StackPanel x:Name="ArcherieSP" Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="{Binding Name,UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock Text=" Prix : "/>
            <TextBlock Text="{Binding Price,UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
        <Button x:Name="SoldierProducerBuyButton" Content="Acheter" Grid.Row="1"/>
    </Grid>
</UserControl>
