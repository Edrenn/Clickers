<UserControl x:Class="Clickers.Views.HealerHouseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Clickers.Views"
             mc:Ignorable="d" 
             d:DesignHeight="1024" d:DesignWidth="1920">
    <UserControl.Resources>
        <ResourceDictionary Source="/Styles/BaseDictionary.xaml"/>
    </UserControl.Resources>
    <Grid Background="BurlyWood">

        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition Height="50"/>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <local:InfoBarUserControl x:Name="InfoBarUC" />

        <TextBlock Text="{Binding Name}" Grid.Row="1" HorizontalAlignment="Center" VerticalAlignment="Center" />

        <Grid Grid.Row="2" Background="BurlyWood">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1171*"/>
                <ColumnDefinition Width="749*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="HeroHealTB" Text="Soigner mes héros" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="911,7,161.304,7.4"/>
            <Button x:Name="HeroHealButton"  Style="{StaticResource MenuButton}" Margin="669,30,42.2,10.4" Grid.RowSpan="2" Width="Auto">
                <Button.Content>
                    <Grid>
                        <Image Source="/Assets/Image/Bouton.png"/>
                        <TextBlock Text="Sélectionner un héros" HorizontalAlignment="Center"  FontSize="20" VerticalAlignment="Center" Style="{StaticResource TitlePolice}" />
                    </Grid>
                </Button.Content>
            </Button>
        </Grid>

        <local:ToCastleButtonUserControl Grid.Row="1" Grid.RowSpan="2" HorizontalAlignment="Left" VerticalAlignment="Top"/>

        <Grid Grid.Row="3" Background="BurlyWood">
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="ObjectListTB" Text="Objets" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <ScrollViewer  Grid.Row="1" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Auto" VerticalAlignment="Center" MaxWidth="750">
                <StackPanel x:Name="ItemsSP" Orientation="Horizontal"></StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</UserControl>
