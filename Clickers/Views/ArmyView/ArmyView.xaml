<UserControl x:Class="Clickers.Views.ArmyView.ArmyView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Clickers.Views"
      xmlns:CV="clr-namespace:Clickers.Views"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300">

    <Grid  Background="BurlyWood">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="0.2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <CV:InfoBarUserControl Grid.Row="0" />
        <local:ToCastleButtonUserControl Grid.Row="1" HorizontalAlignment="Left" />
        <StackPanel x:Name="Units" Grid.Row="2" Orientation="Horizontal"/>
    </Grid>
</UserControl>
